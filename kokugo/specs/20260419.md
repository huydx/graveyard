# Multi-user (tenant = user)

## Model
- **Tenant = one logged-in user.** Data and settings are scoped per user.
- **Gemini key:** stored per user in the DB only; **no** environment-variable fallback for that key (tenant model).

## Ship list
- Login screen; **no** registration / signup for now.
- **Seed:** `admin@admin.com`, `admin1@admin.com` — password `123123` (bootstrap / dev).
- **Migration:** existing data without a user → `admin@admin.com`.
- **UX:** unchanged after login, except new login (and anything session requires).
